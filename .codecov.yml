coverage:
  status:
    project:
      default:
        target: auto
        threshold: 1%  # Allow  amount of decrease
    patch:
      default:
        target: auto
        threshold: 1%  # Allow  amount of decrease